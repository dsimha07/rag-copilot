"""Read/write the index manifest — the bridge for Project 7.

Records embedding model + version, chunking strategy + params, per-doc content
hash, last_modified, chunk counts, timestamp.
"""
# TODO: implement
